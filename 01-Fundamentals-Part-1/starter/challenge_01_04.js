
// let bill = Number(prompt('Insert the total amount of the bill: '));
// let tip;

// (bill >= 50 && bill <= 300) ? tip = (bill * 0.15) : tip = (bill * 0.20);

// totalBill = bill + tip;

// console.log(`The bill was ${bill} and the tip was ${tip}, so the total bill is ${totalBill}`);
